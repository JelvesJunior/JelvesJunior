<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CreatorApp.LoginPage"
             Title="Entrar">
    <ScrollView>
        <VerticalStackLayout Padding="32" Spacing="24">
            <Image Source="dotnet_bot.png" HeightRequest="120" HorizontalOptions="Center" />
            <Label Text="Bem-vindo, criador!" FontSize="24" HorizontalOptions="Center" />
            <Entry Placeholder="E-mail" Text="{Binding Email}" Keyboard="Email" />
            <Entry Placeholder="Senha" Text="{Binding Password}" IsPassword="True" />
            <Button Text="Entrar" Clicked="OnLoginClicked" />
            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
